@page
@model FaulknerCountyMuseumGallery.Pages.Artists.DetailsModel

@{
    ViewData["Title"] = "Details";
}

<div  id="page_title">Artist Details</div>

<div class="bb">
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Artist.Name)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Artist.Name)
        </dd>
    </dl>
    <div>
    <a class="searchb" asp-page="./Edit" asp-route-id="@Model.Artist?.ID">Edit</a>
    <a class="searchb" asp-page="./Index">Back to List</a>
</div>

</div>