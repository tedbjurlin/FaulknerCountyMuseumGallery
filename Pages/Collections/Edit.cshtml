@page
@model FaulknerCountyMuseumGallery.Pages.Collections.EditModel

@{
    ViewData["Title"] = "Edit";
}

<div id="title" style="text-align: center;">Edit Collection</div>

<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Collection.ID" />
            <div class="form-group">
                <label asp-for="Collection.Name" class="control-label"></label>
                <input asp-for="Collection.Name" class="form-control" />
                <span asp-validation-for="Collection.Name" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="./Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
